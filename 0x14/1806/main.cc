#include<bits/stdc++.h>
using namespace std;

int N;
int S;
int arr[100002];

int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin>>N>>S;
	for(int i=1;i<=N;i++)
		cin>>arr[i];
	sort(arr+1,arr+N+1);

	




